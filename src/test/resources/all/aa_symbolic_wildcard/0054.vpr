// Any copyright is dedicated to the Public Domain.
// http://creativecommons.org/publicdomain/zero/1.0/

method m1()
{
    var k: Perm
    //:: ExpectedOutput(consistency.error)
    k := sWildcard

    //:: ExpectedOutput(consistency.error)
    assert sWildcard < k
}

method m2()
{
    var k: Perm
    //:: ExpectedOutput(consistency.error)
    k := sWildcard
}

field f: Int

method m3(obj: Ref)
    requires acc(obj.f, sWildcard)
    ensures acc(obj.f, sWildcard)
{
    var i: Int := 4
    while (i > 0)
        invariant acc(obj.f, sWildcard)
    {
        i := i - obj.f
    }
}

