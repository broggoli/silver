// Any copyright is dedicated to the Public Domain.
// http://creativecommons.org/publicdomain/zero/1.0/

field f: Int

predicate P(x: Ref, p: Perm) {
  true
}

method test(x: Ref)
  // requires acc(P, sWildcard) /* Rejected by the parser */
  //:: ExpectedOutput(typechecker.error)
  requires acc(P(), sWildcard)
  //:: ExpectedOutput(typechecker.error)
  requires acc(P(x), sWildcard)
  //:: ExpectedOutput(typechecker.error)
  requires acc(P(9), sWildcard)
  //:: ExpectedOutput(typechecker.error)
  requires acc(P(x, x))
  requires acc(P(x, sWildcard)) /* OK */
{}
